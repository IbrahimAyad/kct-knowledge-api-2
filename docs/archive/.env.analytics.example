# Analytics Integration - Environment Variables
# Copy these to your .env file and fill in the values

# ============================================
# Google Analytics 4 (GA4) Configuration
# ============================================

# GA4 Property ID (found in GA4 Admin > Property Settings)
# Example: 5979716763
GA4_PROPERTY_ID=your_property_id_here

# Option 1: Service Account JSON File Path (Recommended for production)
# Create a service account in Google Cloud Console with Analytics Data API access
# Download the JSON key file and provide the path
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Option 2: Service Account JSON as Environment Variable (For cloud deployments)
# Paste the entire JSON content of your service account key
# GA4_SERVICE_ACCOUNT_JSON='{"type":"service_account","project_id":"...","private_key":"..."}'

# Option 3: Individual Credentials (Alternative)
# GA4_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
# GA4_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# ============================================
# Shopify Configuration
# ============================================

# Your Shopify store URL (without https://)
# Example: kctmenswear.myshopify.com or kctmenswear.com
SHOPIFY_STORE_URL=your_store.myshopify.com

# Shopify Admin API Access Token
# Generate in Shopify Admin > Apps > Develop apps > Create an app
# Required scopes: read_orders, read_products, read_customers, read_analytics
SHOPIFY_ADMIN_ACCESS_TOKEN=shpat_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Shopify API Version (optional, defaults to latest)
# SHOPIFY_API_VERSION=2024-01

# ============================================
# How to Set Up
# ============================================

# 1. Google Analytics 4 Setup:
#    a. Go to https://console.cloud.google.com
#    b. Create a new project or select existing
#    c. Enable "Google Analytics Data API"
#    d. Create a Service Account with "Viewer" role
#    e. Download JSON key
#    f. In GA4 Admin > Property Access Management, add service account email with "Viewer" role
#    g. Copy your GA4 Property ID from GA4 Admin > Property Settings
#    h. Set GA4_PROPERTY_ID and either GOOGLE_APPLICATION_CREDENTIALS or GA4_SERVICE_ACCOUNT_JSON

# 2. Shopify Setup:
#    a. Go to Shopify Admin > Settings > Apps and sales channels
#    b. Click "Develop apps" > "Create an app"
#    c. Configure Admin API scopes: read_orders, read_products, read_customers, read_analytics
#    d. Install the app on your store
#    e. Copy the Admin API access token
#    f. Set SHOPIFY_STORE_URL and SHOPIFY_ADMIN_ACCESS_TOKEN

# 3. Test the setup:
#    npm run build
#    npm start
#    curl http://localhost:3000/api/analytics/health

# ============================================
# Example URLs for Testing
# ============================================

# Health check: GET /api/analytics/health
# Dashboard: GET /api/analytics/dashboard?days=7
# Traffic: GET /api/analytics/traffic?days=7
# Sales: GET /api/analytics/sales?days=7
# Recommendations: GET /api/analytics/recommendations?days=7
# Real-time: GET /api/analytics/realtime
# Track event: POST /api/analytics/track
